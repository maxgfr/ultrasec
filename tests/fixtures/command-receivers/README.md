Fixture for issue #10 (defect 2): the `command` sink must corroborate an
ambiguous callee before it fires. Every `fp-*` file is a FALSE POSITIVE the
engine used to rate CRITICAL CWE-78; every `tp-*` file is a real command
sink that must keep firing.
